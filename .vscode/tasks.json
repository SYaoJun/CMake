{
    "tasks": [
        {
            "type": "shell",
            "label": "make_1",
            "command": "cmake -DCMAKE_BUILD_TYPE=Debug .. && make -j 8",
            "options": {
                "cwd": "${workspaceFolder}/build/"
            },
        }
    ],
    "version": "2.0.0"
}